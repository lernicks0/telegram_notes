# 803 目标中心部署说明

## 网站信息

- 域名：`goal.lernicks.cn`
- 端口：1152
- PM2 名称：`goal-server`
- 服务器目录：`/root/goal/`
- 数据文件：`/root/goal/goal-data.json`

## 权限规则

- 验证门与 PK、积分中心一致。
- 未登录用户无法读取私密名单和目标；登录后普通同学只能添加、修改和删除自己的目标。
- 界面锁定后，普通用户只能查看，管理员仍可以修改。
- 创建和删除考试场次、锁定与解除锁定，只允许管理员操作。
- 私密名单中标记为学生管理员或老师的账号是管理员，网站不再使用管理员密钥。
- 老师使用账号 `ls`，只负责管理，不计入参与同学人数，也不设置考试目标。
- 统一账号服务监听本机 1153 端口，完整部署方法见 `DEPLOY-CLASS-ACCOUNTS.md`。

## 数据保护

更新程序时不要删除或覆盖 `goal-data.json`。它保存考试场次、全班目标和锁定状态。

## Cloudflare

增加一条 A 记录：

- 名称：`goal`
- 内容：`124.223.201.40`
- 代理状态：已代理（橙色云朵）
